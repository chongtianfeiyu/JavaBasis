<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	
    <!-- 
    	Bean的作用域：
    	使用 bean 的scope属性来配置 bean 的作用域
    	singleton：容器在初始化时创建bean实例，在整个容器的生命周期内只创建一次。【单例模式】，这个是默认值；
    	prototype：容器在初始化时不创建bean的实例，而是在每次获取bean时创建一个新的bean的实例。【原型模式】；
    	
     -->
    <bean id="car" class="com.startcaft.spring.autowire.Car" scope="singleton">
        <property name="brand" value="Audi"></property>
        <property name="price" value="300000.00"></property>
    </bean>

</beans>
